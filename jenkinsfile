pipeline {
    agent any

    stages {
        stage('Checkout SCM') {
            steps {
                checkout scm
            }
        }
        stage('Build') {
            steps {
                echo 'Building the codes...'
                // Add your build commands here
            }
            post {
                success {
                    emailext (
                        to: "nimeshmakevitage@gmail.com",
                        subject: "Build Successful: ${env.JOB_NAME} - Build #${env.BUILD_NUMBER}",
                        body: "The build was successful!",
                        attachLog: true // Attach the log files
                    )
                }
                failure {
                    emailext (
                        to: "nimeshmakevitage@gmail.com",
                        subject: "Build Failed: ${env.JOB_NAME} - Build #${env.BUILD_NUMBER}",
                        body: "The build failed.",
                        attachLog: true // Attach the log files
                    )
                }
            }
        }
        stage('Unit and Integration Tests') {
            steps {
                echo 'Running unit and integration tests...'
                // Add your testing commands here
            }
            post {
                success {
                    emailext (
                        to: "nimeshmakevitage@gmail.com",
                        subject: "Unit and Integration Tests Successful: ${env.JOB_NAME} - Build #${env.BUILD_NUMBER}",
                        body: "The unit and integration tests were successful!",
                        attachLog: true // Attach the log files
                    )
                }
                failure {
                    emailext (
                        to: "nimeshmakevitage@gmail.com",
                        subject: "Unit and Integration Tests Failed: ${env.JOB_NAME} - Build #${env.BUILD_NUMBER}",
                        body: "The unit and integration tests failed.",
                        attachLog: true // Attach the log files
                    )
                }
            }
        }
        stage('Code Analysis') {
            steps {
                echo 'Analyzing code...'
                // Add your code analysis commands here
            }
            post {
                success {
                    emailext (
                        to: "nimeshmakevitage@gmail.com",
                        subject: "Code Analysis Successful: ${env.JOB_NAME} - Build #${env.BUILD_NUMBER}",
                        body: "The code analysis was successful!",
                        attachLog: true // Attach the log files
                    )
                }
                failure {
                    emailext (
                        to: "nimeshmakevitage@gmail.com",
                        subject: "Code Analysis Failed: ${env.JOB_NAME} - Build #${env.BUILD_NUMBER}",
                        body: "The code analysis failed.",
                        attachLog: true // Attach the log files
                    )
                }
            }
        }
        stage('Security Scan') {
            steps {
                echo 'Performing security scan...'
                // Add your security scan commands here
            }
            post {
                success {
                    emailext (
                        to: "nimeshmakevitage@gmail.com",
                        subject: "Security Scan Successful: ${env.JOB_NAME} - Build #${env.BUILD_NUMBER}",
                        body: "The security scan was successful!",
                        attachLog: true // Attach the log files
                    )
                }
                failure {
                    emailext (
                        to: "nimeshmakevitage@gmail.com",
                        subject: "Security Scan Failed: ${env.JOB_NAME} - Build #${env.BUILD_NUMBER}",
                        body: "The security scan failed.",
                        attachLog: true // Attach the log files
                    )
                }
            }
        }
        stage('Deploy to Staging') {
            steps {
                echo 'Deploying to staging...'
                // Add your deployment commands here
            }
            post {
                success {
                    emailext (
                        to: "nimeshmakevitage@gmail.com",
                        subject: "Deployment to Staging Successful: ${env.JOB_NAME} - Build #${env.BUILD_NUMBER}",
                        body: "The deployment to staging was successful!",
                        attachLog: true // Attach the log files
                    )
                }
                failure {
                    emailext (
                        to: "nimeshmakevitage@gmail.com",
                        subject: "Deployment to Staging Failed: ${env.JOB_NAME} - Build #${env.BUILD_NUMBER}",
                        body: "The deployment to staging failed.",
                        attachLog: true // Attach the log files
                    )
                }
            }
        }
        stage('Integration Tests on Staging') {
            steps {
                echo 'Running integration tests on staging...'
                // Add your integration testing commands here
            }
            post {
                success {
                    emailext (
                        to: "nimeshmakevitage@gmail.com",
                        subject: "Integration Tests on Staging Successful: ${env.JOB_NAME} - Build #${env.BUILD_NUMBER}",
                        body: "The integration tests on staging were successful!",
                        attachLog: true // Attach the log files
                    )
                }
                failure {
                    emailext (
                        to: "nimeshmakevitage@gmail.com",
                        subject: "Integration Tests on Staging Failed: ${env.JOB_NAME} - Build #${env.BUILD_NUMBER}",
                        body: "The integration tests on staging failed.",
                        attachLog: true // Attach the log files
                    )
                }
            }
        }
        stage('Deploy to Production') {
            steps {
                echo 'Deploying to production...'
                // Add your production deployment commands here
            }
            post {
                success {
                    emailext (
                        to: "nimeshmakevitage@gmail.com",
                        subject: "Deployment to Production Successful: ${env.JOB_NAME} - Build #${env.BUILD_NUMBER}",
                        body: "The deployment to production was successful!",
                        attachLog: true // Attach the log files
                    )
                }
                failure {
                    emailext (
                        to: "nimeshmakevitage@gmail.com",
                        subject: "Deployment to Production Failed: ${env.JOB_NAME} - Build #${env.BUILD_NUMBER}",
                        body: "The deployment to production failed.",
                        attachLog: true // Attach the log files
                    )
                }
            }
        }
    }
}
